import React from 'react';
import { YEAR } from '../../config';

const Footer = () => {
  return (
    <footer>
      { `Gerenciamento de reviews @ ${YEAR}`}
    </footer>
  )
}

export default Footer;